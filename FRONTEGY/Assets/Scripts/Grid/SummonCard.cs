using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class SummonCard : Card
{  // UNUSED
    
    public SummonCard(Grid grid, Role role, Player owner) : base(grid, null, owner)
    {
        Debug.LogError("TOLD YOU NOT TO USE THIS SHIT");
        if (role == null) Debug.LogError("IllegalArgumentException");

        Spell spell = new SummonSpell(role);
        List<Spell> spells = new List<Spell> { spell };
        SpellCollection spellCollection = new SpellCollection(spells, "summon");
        List<SpellCollection> spellCollections = new List<SpellCollection> { spellCollection };
        
        //setSpellCollections(spellCollections);
    }
    
}
*/
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UICard
{/* UNUSED
    // may reach cardPhy from card
    private Card card;

    public UICard(Card card)
    {
        if (card == null) Debug.LogError("IllegalArgumentException");
        this.card = card;
        // store index and have a getter method for UI.cardUICount
    }
    */
}
